spring:
  data:
    mongodb:
      uri: mongodb://admin:password@localhost:27017/event_registration?authSource=admin
      auto-index-creation: true

logging:
  level:
    org.springframework.data.mongodb:
      core.MongoTemplate: DEBUG